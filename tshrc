# ~/.tshrc
#
# Example run-config file.

alias ls='ls --color'
alias l='ls -l'

alias cd..='cd ..'
alias pw='pwd'
alias ud='grep -i dor /etc/passwd'
alias dt='date'
alias ifc='ifconfig'

echo Welcome to D_SHELL .
